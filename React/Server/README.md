# Node Backend Client for Devclub SSO

To set up the server running, do `npm install`, then to start the server use `npm start`.

To run in hot reload mode use `npm run devStart`. 

Server runs on port `8000`
